frontend:
  build:
    image: aubron/frontend
    dockerfile: frontend/Dockerfile
  encrypted_env_file: frontend.env.encrypted
  cached: true
  volumes_from:
    - data
data:
  image: busybox
  volumes:
    - ./tmp/app:/app/build
